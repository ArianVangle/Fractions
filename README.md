# 🧮 Fractions — Класс для работы с дробями на C++

`Fractions` — это простой заголовочный класс на C++ для точной арифметики с рациональными числами (обыкновенными дробями). Класс поддерживает арифметические операции и сравнения как между дробями, так и с целыми числами (`int`) и вещественными числами (`double`).

---

## 📦 Возможности

- **Точные арифметические операции**: сложение, вычитание, умножение и деление дробей.
- **Автоматическое сокращение**: все дроби при создании и в результате операций приводятся к несократимому виду с помощью НОД (наибольшего общего делителя).
- **Операции с разными типами**: поддержка смешанных выражений, например `Fraction + int`, `double - Fraction` и т.д.
- **Полная поддержка сравнений**: все 6 операторов сравнения (`==`, `!=`, `<`, `>`, `<=`, `>=`) для комбинаций `Fractions`, `int` и `double`.
- **Заголовочный файл**: достаточно подключить `Fractions.hpp` — компиляция без дополнительных библиотек.

---

## 🚀 Быстрый старт

```cpp
#include "Fractions.hpp"
#include <iostream>

int main() {
    Fractions a(1, 2);   // 1/2
    Fractions b(3, 4);   // 3/4

    Fractions sum = a + b;        // 5/4
    Fractions product = a * 3;    // 3/2
    double d = sum + 0.25;        // 1.5

    std::cout << "a = " << a.to_float() << "\n";       // 0.5
    std::cout << "a + b = " << sum.to_float() << "\n"; // 1.25
    std::cout << "a < b? " << (a < b) << "\n";         // 1 (true)

    return 0;
}
```

---

## 📐 Арифметические операции

Поддерживаются все основные операторы:

| Операция | Примеры |
|--------|--------|
| Сложение | `f + g`, `f + 5`, `2.5 + f` |
| Вычитание | `f - g`, `f - 3`, `10.0 - f` |
| Умножение | `f * g`, `f * 2`, `1.5 * f` |
| Деление | `f / g`, `f / 4`, `8.0 / f` |

Результат операций между дробями — всегда новая дробь в несократимом виде.  
Операции с `double` возвращают `double`.

---

## 🔍 Сравнение

Можно сравнивать:

- `Fractions` с `Fractions`
- `Fractions` с `int`
- `Fractions` с `double`
- `int` с `Fractions`
- `double` с `Fractions`

Примеры:
```cpp
Fractions f(1, 3);

bool eq1 = (f == Fractions(2, 6));  // true (если дроби канонические!)
bool eq2 = (f == 0.333);            // может быть false из-за округления!
bool less = (2 < f);                // false
```

> ⚠️ **Важно**: сравнение через `to_float()` (внутренне используется для `double`) может быть **неточным** из-за ошибок представления вещественных чисел. Для надёжного сравнения дробей друг с другом рекомендуется использовать только дроби в канонической форме.

---

## 📏 Методы

| Метод | Описание |
|------|--------|
| `getX()` | Возвращает числитель (в канонической форме) |
| `getY()` | Возвращает знаменатель (в канонической форме) |
| `to_int()` | Целочисленное деление числителя на знаменатель |
| `to_float()` | Преобразует дробь в `float` |

---

## 🔧 Реализация

- Все дроби хранятся в **несократимом виде** благодаря автоматическому вызову `gcd` в конструкторе.
- Знак дроби хранится в **числителе**.
- Используются вспомогательные функции:
  - `gcd(a, b)` — наибольший общий делитель (всегда положительный)
  - `lcm(a, b)` — наименьшее общее кратное



---

## 📄 Лицензия

Без лицензии — свободное использование в учебных и коммерческих проектах.
